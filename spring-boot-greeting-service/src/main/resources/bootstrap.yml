# The bootstrap.yml file should only contain the information needed to reach out to Eureka
# And resolve the config server via Eureka

spring:
  application:
    name: greeting-service

  cloud:
    config:
      discovery:
        enabled: true
        service-id: config-server