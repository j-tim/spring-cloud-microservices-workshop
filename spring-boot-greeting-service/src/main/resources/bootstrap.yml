# The bootstrap.yml file should only contain the information needed to reach out to the config server!
# or to Eureka (see exercise 3)

spring:
  application:
    name: greeting-service

  cloud:
    config:
      uri: http://localhost:8888